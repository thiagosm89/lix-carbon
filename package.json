{
  "name": "lixcarbon-platform",
  "version": "1.0.0",
  "description": "Plataforma de Crédito de Carbono através da Coleta de Lixo",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["carbon-credit", "sustainability", "recycling"],
  "author": "LixCarbon",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

